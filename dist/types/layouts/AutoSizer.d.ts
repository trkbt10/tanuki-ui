import * as React from "react";
export declare const AutoSizer: {
    (props: {
        children: (size: {
            width: number;
            height: number;
        }) => React.ReactNode;
    }): React.JSX.Element;
    displayName: string;
};
