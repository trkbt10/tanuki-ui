import { MediaSourceProps } from '../elements/Media';
import * as React from "react";
export declare const StickyHeader: {
    ({ cover, children }: {
        cover: MediaSourceProps;
        children: React.ReactNode;
    }): React.JSX.Element;
    displayName: string;
};
