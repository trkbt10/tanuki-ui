import { default as React } from 'react';
import { PopupLayoutProps } from './parts/PopupLayout';
export declare const Modal: {
    ({ open, onClose, children, potal }: PopupLayoutProps): React.JSX.Element;
    displayName: string;
};
